# flymito.com

Web de **Flymito** — muebles y objetos hechos con piezas de avión reales.

Sitio estático: `index.html` + `img/`. Sin dependencias, sin proceso de compilación.
Se publica solo con GitHub Pages en cada push a `main`.
